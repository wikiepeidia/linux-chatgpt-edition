---
quick_id: 260827-se6
phase: quick-260827-se6-deadline-mvp
plan: "01"
type: execute
mode: quick
tracer_mode: false
wave: 1
depends_on: []
autonomous: true
files_modified:
  - .gitattributes
  - build.ps1
  - builder/inputs.json
  - builder/profiles/mkimg.300k.sh
  - builder/apkovl/genapkovl-300k.sh
  - builder/apkovl/rootfs/etc/inittab
  - builder/apkovl/rootfs/etc/local.d/300k.start
  - builder/apkovl/rootfs/etc/profile.d/300k-session.sh
  - builder/apkovl/rootfs/etc/doas.d/300k.conf
  - builder/apkovl/rootfs/home/chatgpt/.xinitrc
  - builder/apkovl/rootfs/home/chatgpt/.config/openbox/rc.xml
  - builder/apkovl/rootfs/usr/local/bin/300k-runtime
  - builder/apkovl/rootfs/usr/local/sbin/300k-power
  - builder/apkovl/rootfs/usr/local/lib/300k/content.tcl
  - builder/apkovl/rootfs/usr/local/lib/300k/ui.tcl
  - scripts/linux/run-build.sh
  - scripts/linux/inspect-deadline-iso.sh
  - scripts/host/Invoke-DeadlineSmoke.ps1
  - tests/deadline/run.ps1
  - docs/DEADLINE-MVP.md
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A clean committed source tree produces one hash-qualified x86_64 Alpine ISO candidate without changing the existing LATEST pointer."
    - "A direct BIOS optical boot with -nic none reaches an original full-screen ChatGPT-inspired parody UI as an unprivileged live user."
    - "The UI persistently says it is an unofficial offline local parody; composer text is data only and cannot become a shell command."
    - "The UI contains at least twelve safe local comedy intents plus Help, About, factual System Info, reboot, shutdown, and a fixed terminal button."
    - "Ctrl+Alt+T and the UI terminal button launch xterm with a constant /bin/ash -l argv; a one-shot xterm probe proves a non-root /dev/pts shell, command output, file I/O, and a real failing exit code."
    - "Only after one bounded direct BIOS smoke records ordered ROOTFS_READY, X_READY, UI_READY, TERM_EXEC_OK, a nonblank screenshot, and the exact ISO SHA/bytes may LATEST change."
    - "The old last-known-good ISO remains byte-identical, and every unexecuted compatibility/release claim is labeled deferred."
  artifacts:
    - path: "builder/apkovl/genapkovl-300k.sh"
      provides: "Deterministic Alpine diskless overlay generator"
    - path: "builder/apkovl/rootfs/usr/local/lib/300k/ui.tcl"
      provides: "Original full-screen offline parody UI"
    - path: "builder/apkovl/rootfs/usr/local/lib/300k/content.tcl"
      provides: "Prompt-as-data local intent and comedy engine"
    - path: "builder/apkovl/rootfs/usr/local/bin/300k-runtime"
      provides: "Guarded session, fixed terminal launcher, serial stages, and PTY proof"
    - path: "scripts/linux/inspect-deadline-iso.sh"
      provides: "Fast hash and boot-structure candidate inspection"
    - path: "scripts/host/Invoke-DeadlineSmoke.ps1"
      provides: "Single-attempt direct BIOS QEMU smoke, evidence capture, and gated promotion"
    - path: "tests/deadline/run.ps1"
      provides: "Fast runtime, boundary, builder, publication, and evidence tests"
    - path: "docs/DEADLINE-MVP.md"
      provides: "Truthful verified/deferred capability contract"
  key_links:
    - from: "builder/profiles/mkimg.300k.sh"
      to: "builder/apkovl/genapkovl-300k.sh"
      via: "profile_300k_deadline sets hostname and the pinned aports apkovl script path"
      pattern: "profile_300k_deadline|genapkovl-300k"
    - from: "builder/apkovl/rootfs/etc/profile.d/300k-session.sh"
      to: "builder/apkovl/rootfs/usr/local/bin/300k-runtime"
      via: "tty1-only, empty-DISPLAY startx guard"
      pattern: "tty1|300k-session"
    - from: "builder/apkovl/rootfs/usr/local/lib/300k/ui.tcl"
      to: "builder/apkovl/rootfs/usr/local/lib/300k/content.tcl"
      via: "composer submits text only to the sourced reply function"
      pattern: "source.*content[.]tcl|reply_for"
    - from: "builder/apkovl/rootfs/home/chatgpt/.config/openbox/rc.xml"
      to: "builder/apkovl/rootfs/usr/local/bin/300k-runtime"
      via: "Ctrl+Alt+T invokes the same constant terminal launcher as the UI button"
      pattern: "C-A-t|300k-terminal"
    - from: "scripts/linux/run-build.sh"
      to: "scripts/linux/inspect-deadline-iso.sh"
      via: "DeadlineMvp branch selects fast structural inspection while Bootstrap retains recursive inspection"
      pattern: "300k_deadline|inspect-deadline-iso"
    - from: "scripts/host/Invoke-DeadlineSmoke.ps1"
      to: "dist/LATEST.json"
      via: "exact candidate hash plus passing one-attempt evidence gates atomic pointer replacement"
      pattern: "DeadlineSmokeEvidence|LATEST[.]json"
---

<objective>
Ship the deadline vertical slice: one bootable Alpine x86_64 ISO that lands in a funny, original ChatGPT-inspired offline desktop and exposes a genuine local terminal, with one narrow but credible BIOS proof.

Purpose: The remaining time is spent on the visible product and the direct boot proof instead of completing Phase 01's multi-hour assurance program. The existing build foundation and last-known-good artifact remain intact.

Output: committed runtime/UI and deadline-build code, one staged ISO candidate, one direct BIOS smoke evidence bundle, and—only on a passing smoke—an atomically updated LATEST pointer.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-reproducible-build-foundation/01-01-SUMMARY.md
@build.ps1
@builder/inputs.json
@builder/profiles/mkimg.300k.sh
@scripts/linux/run-build.sh
@scripts/linux/inspect-iso.sh
@scripts/host/Invoke-QemuBackend.ps1
@scripts/host/Invoke-CheckedProcess.ps1
@tests/phase1/run.ps1
</context>

<scope_lock>
## Included deadline contract

- Alpine 3.24.1 x86_64 diskless ISO, built by the existing QEMU builder and the pinned aports commit.
- Unprivileged `chatgpt` live user, guarded tty1 `startx`, Openbox, Tcl/Tk UI, xterm, BusyBox `ash`, and serial readiness markers.
- Original project-drawn UI only; no copied OpenAI logo/assets and no claim of affiliation.
- Fully local behavior with no API, account, credential, telemetry, Codex, model runtime, or cloud dependency.
- One direct BIOS optical QEMU smoke with `-nic none`, exact hash/bytes, ordered markers, PTY proof, and nonblank screenshot.

## Explicitly deferred; do not implement or claim in this quick task

- Phase 01-02's final recursive security gate and its exhaustive archive/security matrix.
- Phase 01-03 and Docker adapter/parity.
- Runtime UEFI boot verification, raw-USB/media verification, and broad hardware support.
- A second build for byte-for-byte reproducibility.
- Size optimization or achievement of the 100 MB stretch target.
- Full release ceremony/matrix. This result is a deadline BIOS optical MVP with measured bytes, not a general release certification.

Do not reset, rebase, squash, or revert the current commit history. The prior publication is immutable until the final smoke passes:

- `dist/p01-172f13500b74/300k-bootstrap-x86_64-2c968ee3a0c6.iso`
- SHA-256 `2c968ee3a0c687bd0d779247b7b595494a6a9870308387a07572796fd46d0678`
- `69,206,016` bytes
</scope_lock>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the complete offline graphical/runtime slice and deterministic apkovl</name>
  <files>.gitattributes, builder/apkovl/genapkovl-300k.sh, builder/apkovl/rootfs/etc/inittab, builder/apkovl/rootfs/etc/local.d/300k.start, builder/apkovl/rootfs/etc/profile.d/300k-session.sh, builder/apkovl/rootfs/etc/doas.d/300k.conf, builder/apkovl/rootfs/home/chatgpt/.xinitrc, builder/apkovl/rootfs/home/chatgpt/.config/openbox/rc.xml, builder/apkovl/rootfs/usr/local/bin/300k-runtime, builder/apkovl/rootfs/usr/local/sbin/300k-power, builder/apkovl/rootfs/usr/local/lib/300k/content.tcl, builder/apkovl/rootfs/usr/local/lib/300k/ui.tcl, tests/deadline/run.ps1</files>
  <behavior>
    - Runtime-static test: the apkovl generator accepts exactly one safe hostname, uses a private temporary tree with cleanup, installs explicit modes, emits a sorted package world, normalizes owner/group/mtime, uses deterministic gzip framing, and cannot include private-key or host-path material.
    - Session test: tty1 autologin reaches the locked `chatgpt` account, starts X only when `DISPLAY` is empty, retries a bounded number of times, then falls back to an interactive rescue shell instead of looping.
    - Stage test: the authored boot path contains exactly one producer for each stable marker `300K_STAGE=ROOTFS_READY`, `300K_STAGE=X_READY`, and `300K_STAGE=UI_READY`; the terminal proof is the sole producer of `300K_STAGE=TERM_EXEC_OK`.
    - Composer test: shell-looking prompts and metacharacters remain ordinary strings, select a local reply, and never reach Tcl `eval`, process launch, file open, dynamic source/load, socket, or HTTP behavior.
    - Content test: at least twelve unique reviewed intents/replies exist, an unknown-input fallback exists, random selection is seedable, and all effect/action identifiers belong to a closed non-executable allowlist.
    - Terminal test: both visible UI action and Openbox `Ctrl+Alt+T` resolve to one fixed launcher whose normal argv is `xterm -e /bin/ash -l`; prompt text is not an argument source.
    - PTY test contract: the launcher's internal proof mode uses a real xterm, requires nonzero uid and `/dev/pts/*`, proves a command result, creates and rereads a disposable file, captures exit code 1 from a failing command, then emits those facts in `TERM_EXEC_OK`.
    - UI test: the top-level UI is full-screen, draws original 300K artwork in Tcl/Tk, keeps an always-visible `UNOFFICIAL PARODY / OFFLINE / LOCAL SCRIPT / NO OPENAI SERVICE` notice, and exposes Terminal, Help, About, System Info, Reboot, and Shutdown controls.
  </behavior>
  <action>
Create `tests/deadline/run.ps1 -Scope RuntimeStatic` first and make each behavior above fail against the absent runtime. Keep it dependency-free on the Windows host: parse Tcl and shell ownership seams structurally, parse Openbox XML with .NET XML APIs, enforce LF/no-BOM/no-control-character text, validate exact fixed command tokens, and parse the content records rather than counting comments.

Implement the Alpine overlay as authored files plus `genapkovl-300k.sh`. The generator follows the pinned aports callback (`genapkovl-300k.sh <hostname>` writes `<hostname>.apkovl.tar.gz` in its current directory), stages under `mktemp -d`, installs every file with an explicit mode, creates only required runlevel links/symlinks, writes `/etc/apk/world` in sorted order, normalizes tar paths/uid/gid/mtime from `SOURCE_DATE_EPOCH`, and compresses with `gzip -9n`. Its world contains the reliable graphical closure: `alpine-base`, `doas`, `eudev`, `font-terminus`, `mesa-dri-gallium`, `openbox`, `tcl`, `tk`, `xf86-input-libinput`, `xinit`, `xorg-server`, and `xterm`. Do not enable a networking service.

Use `300k.start` during the default OpenRC runlevel to create/repair the locked, unprivileged uid/gid 1000 `chatgpt` account and home ownership, verify the required runtime programs and `/dev/pts`, and emit `ROOTFS_READY` to both `/var/log/300k/stages.log` and `/dev/ttyS0`. Preserve tty2 as a normal rescue console. Configure tty1 getty through a fixed autologin subcommand; the profile guard runs the session only on `/dev/tty1` with no existing display. The session performs at most two `startx` attempts, returns to `ash -l` on failure, starts Openbox, emits `X_READY`, and supervises the UI without an unbounded respawn.

Implement `300k-runtime` as a closed multi-call dispatcher for `autologin`, `session`, `terminal`, `terminal-proof`, `serial-stage`, and `system-info`. The normal terminal action must exec the constant argv `xterm -T "300K Terminal" -e /bin/ash -l`. Its internal proof action opens xterm with a fixed proof script path and emits only after the real PTY checks pass. `300k-power` accepts only `reboot` or `poweroff`; two `doas` rules grant the live user only those exact helper invocations, never a root shell or arbitrary argv.

Implement the content engine as a sourceable Tcl module with normalized local intent matching, a seed seam (`300K_SEED` when present), at least twelve original absurd replies, and a safe fallback. The module must contain no process/file/network evaluation primitive. Implement the Tk shell with an original 300K canvas badge, chat transcript, composer, persistent disclaimer, local-only status, and fixed controls. Composer submission calls only the content function. Help/About/System Info are truthful local views; System Info calls only the fixed dispatcher. Reboot/Shutdown request confirmation then invoke their fixed helpers. After `update idletasks` and a positive `winfo ismapped`, emit `UI_READY` and schedule the one-shot xterm proof. Bind both the visible terminal control and Openbox shortcut to the same launcher.

Make `RuntimeStatic` green, review the generated source list for secrets/host paths, and commit this task atomically as `feat(deadline): add offline desktop runtime` before touching the integration path.
  </action>
  <verify>
    <automated>pwsh -NoProfile -File tests/deadline/run.ps1 -Scope RuntimeStatic</automated>
  </verify>
  <done>The deterministic overlay source, live-user/X session, full-screen parody UI, safe local content, fixed terminal/power boundaries, and serial/PTY proof contract all pass fast host tests and are committed independently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a deadline-only build, fast inspection, candidate publication, and direct BIOS smoke path</name>
  <files>build.ps1, builder/inputs.json, builder/profiles/mkimg.300k.sh, scripts/linux/run-build.sh, scripts/linux/inspect-deadline-iso.sh, scripts/host/Invoke-DeadlineSmoke.ps1, tests/deadline/run.ps1, tests/phase1/run.ps1, docs/DEADLINE-MVP.md</files>
  <behavior>
    - Target test: `build.ps1 -Target DeadlineMvp -Backend Qemu` maps to profile `300k_deadline`; the existing Bootstrap target and its recursive validator remain available and unchanged in meaning.
    - Profile test: `profile_300k_deadline` inherits Alpine `profile_virt`, targets only x86_64, sets hostname `300k`, uses the deterministic apkovl callback, keeps BIOS/EFI boot structure, and includes the exact graphical world closure.
    - Deadline branch test: it keeps the existing closed keyring, signed local repository, clean source, offline `apk --no-network`, and hash contracts, but calls neither the hostile recursive inspector self-test nor recursive finished-ISO audit.
    - Fast-inspection test: the deadline inspector hashes the settled ISO, records exact bytes, and requires kernel, initramfs, apkovl, local APK index, BIOS ISOLINUX catalog/loader, and EFI loader/El Torito structure before returning pass.
    - Candidate test: a successful build stages under `dist/.deadline-candidates/<id>` with a closed manifest and does not create, replace, or delete `dist/LATEST.json` or any existing published directory.
    - Promotion test: malformed/missing smoke evidence, wrong ISO hash/bytes, blank screenshot evidence, marker disorder/duplication, root uid, non-PTY tty, or missing command/file/exit facts all fail while a byte fixture proves the prior LATEST pointer is unchanged.
    - Smoke-argv test: the direct runner uses `D:\VM\qemu\qemu-system-x86_64.exe`, BIOS `pc` machine, optical `-cdrom` boot, 1 GiB RAM, standard VGA, serial capture, loopback QMP, `-display none`, and the literal `-nic none`; it attaches no writable guest disk.
    - Smoke-evidence test: one attempt waits for ordered ROOTFS/X/UI/TERM markers, obtains one QMP screendump, validates PPM dimensions and pixel variance, records exact ISO SHA/bytes, then quits only its owned QEMU lease.
    - One-attempt test: the runner creates an attempt record before launch and refuses a second invocation for that candidate, whether the first result passed or failed.
  </behavior>
  <action>
Extend the existing contracts by target instead of weakening the Bootstrap path. Add `DeadlineMvp` to the PowerShell target enum and map it to `300k_deadline`, a `deadline-<request-prefix>` build id, and `deadline_mvp_iso` artifact role. Keep the existing clean-tree guard, source archive identity, signing key outside Git, QEMU transport, closed Alpine/project keyring, signed content-addressed repository, offline package consumption, exact child-process ownership, and finite timeouts.

Add `tcl=8.6.17-r1` to the retained builder packages so the deadline branch can run the content module's display-free hostile-input self-test before mkimage. Add the graphical package names to `requested_image_packages` so the retained local repository contains their exact resolved closure. In `run-build.sh`, derive the allowlisted profile from `BuildRequest.target.profile`, copy the apkovl generator/tree into the builder user's `.mkimage` area with explicit permissions, and invoke the profile selected by the request. Bootstrap must still execute `run_inspector_self_test` and `inspect_iso_artifact`. DeadlineMvp instead runs the Tcl content self-test, mkimage once, and `inspect-deadline-iso.sh`; never synthesize recursive audit fields.

Implement `inspect-deadline-iso.sh` with the already pinned xorriso binary/banner contract. It writes `deadline-inspection.json` only after matching the exact finished ISO SHA/bytes, enumerating required ISO paths, and proving BIOS plus UEFI structural records through xorriso reports. Label its scope `deadline-fast-structural`; explicitly record that recursive content audit and runtime UEFI are not executed. Produce `SHA256SUMS`, `boot-layout.txt`, resolved package/build lock evidence, and a closed candidate manifest tied to the BuildRequest and ISO.

In `build.ps1`, add a target-specific staging validator and candidate publisher. The deadline build may write only `dist/.deadline-candidates/<build-id>/`; it must never call the ordinary LATEST publication function. Add an internal completion function that accepts a closed passing `DeadlineSmokeEvidence`, independently rehashes the candidate and screenshot/serial files, verifies all required facts and deferral labels, atomically renames the candidate directory into `dist/<build-id>`, then replaces `LATEST.json` using a same-directory temporary file and .NET replace/move semantics. Any failure leaves the old pointer and old published directories byte-identical.

Implement `Invoke-DeadlineSmoke.ps1` using the existing checked-process lease primitives and exact owned-process cleanup. It takes one candidate manifest and evidence directory, writes a durable attempt record before launch, refuses reuse, resolves and rehashes the ISO, launches exactly one direct BIOS QEMU process with the tested argv, tails its run-local serial file for the four ordered unique stages, speaks QMP only on a dynamically allocated loopback endpoint, captures exactly one PPM after readiness, checks nonzero dimensions plus nontrivial pixel variance, and issues QMP quit in `finally`. Parse `TERM_EXEC_OK` fields strictly: `uid` must be decimal and nonzero, `user=chatgpt`, tty must match `/dev/pts/<number>`, `command=ok`, `file=ok`, and `exit=1`. Write evidence before requesting promotion. Do not report automated clicking/typing: the proof is the launcher's actual xterm PTY seam, while the visible button/shortcut wiring is covered by fast tests.

Expand `tests/deadline/run.ps1` with `BuildStatic`, `Publication`, and fixture-only `SmokeUnit` scopes. Add only the small Phase 1 unit adjustments needed for the new allowlisted target/inputs, without loosening its negative security assertions. Write `docs/DEADLINE-MVP.md` with the exact build/smoke commands, the offline/unofficial identity, what the terminal proof establishes, and the explicit deferred list from `<scope_lock>`. Run all fast tests, then commit every production/test/doc change atomically as `feat(deadline): add BIOS candidate proof path`. The worktree must be clean before the integration build begins.
  </action>
  <verify>
    <automated>pwsh -NoProfile -File tests/deadline/run.ps1 -Scope AllStatic; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pwsh -NoProfile -File tests/phase1/run.ps1 -Scope Unit</automated>
  </verify>
  <done>The clean committed source exposes a target-isolated fast build path, immutable candidate staging, strict single-attempt BIOS runner, fail-closed promotion, and truthful documentation; all fast tests and the pre-existing Phase 1 unit suite pass without an ISO build.</done>
</task>

<task type="auto">
  <name>Task 3: Build once, execute exactly one direct BIOS ISO smoke, and publish only on pass</name>
  <files>.planning/quick/260827-se6-deadline-mvp-vertical-slice-preserve-cur/260827-se6-SUMMARY.md</files>
  <action>
Treat this as a one-way integration gate. Do not edit production or test code after starting it. Before recording either attempt, require an empty `git status --porcelain --untracked-files=all`, rerun `AllStatic` and the Phase 1 unit suite, run the deadline preflight, confirm the existing LATEST pointer still identifies the exact old ISO listed in `<scope_lock>`, and record byte hashes for both old pointer and old ISO. Confirm the existing build preflight's free-space floor on the state drive and D: rather than starting another recursive scan.

Invoke the deadline QEMU build exactly once from the clean commit:

`pwsh -NoProfile -File build.ps1 -Backend Qemu -Target DeadlineMvp -StateRoot "$env:LOCALAPPDATA\300k-linux" -QemuRoot 'D:\VM\qemu'`

Require a closed candidate manifest, nonzero ISO, exact candidate SHA-256/byte count, passing fast structural inspection, BIOS boot record, kernel/initramfs/apkovl/local-repository paths, and retained build/package evidence. Rehash the old ISO and verify it is unchanged. Before the smoke, prove `dist/LATEST.json` is byte-identical to the pre-build snapshot.

Invoke `scripts/host/Invoke-DeadlineSmoke.ps1` exactly once for that candidate with `-QemuRoot 'D:\VM\qemu'`, a fresh run-local evidence directory, a 15-minute bound, and promotion enabled. There is no retry in this plan. On failure, retain the attempt, serial, QEMU stderr, and any screenshot; leave the candidate quarantined and LATEST byte-identical; report the exact failed contract without claiming a bootable product. On pass, independently verify the evidence names the same ISO hash/bytes and contains ordered unique `ROOTFS_READY -> X_READY -> UI_READY -> TERM_EXEC_OK`, non-root `chatgpt`, `/dev/pts/<n>`, command/file success, exit code 1, `-nic none`, BIOS optical media, and a nonblank screenshot. Confirm the new LATEST pointer matches the promoted directory while the old ISO remains byte-identical.

Create the quick SUMMARY with the exact final ISO path, SHA-256, bytes/MiB, source commit, QEMU command line, marker line numbers, terminal fields, screenshot path/hash/dimensions/variance result, old-LKG preservation result, and a verified-versus-deferred table. State the 100 MB target as met or missed using the measured bytes; do not reinterpret it. Commit only the plan/summary evidence after the generated artifact checks complete.
  </action>
  <verify>
    <automated>pwsh -NoProfile -File tests/deadline/run.ps1 -Scope Evidence -QemuRoot 'D:\VM\qemu' -LatestPath dist/LATEST.json</automated>
  </verify>
  <done>Either (a) one exact BIOS optical candidate is promoted with complete truthful evidence and the prior ISO preserved, or (b) the sole attempt is retained as a failure and LATEST remains byte-identical; no unsupported lane or release claim is made.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Composer text -> Tcl content engine | Arbitrary user text crosses into reply selection and must never become code, a path, or argv. |
| Live user -> privileged power helper | Unprivileged UI may request only exact reboot/poweroff operations. |
| Committed source -> QEMU builder | Existing clean-source, key, repository, and offline package trust contracts remain authoritative. |
| Candidate directory -> LATEST | Unpublished bytes cross into the public artifact namespace only after exact-hash BIOS evidence. |
| Host smoke runner -> QEMU/QMP | One owned process and an ephemeral loopback control endpoint produce bounded evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-DL-01 | Tampering / Elevation | Composer and Tcl callbacks | high | mitigate | Pure prompt-to-reply function; no evaluation/file/network primitives; fixed UI action callbacks; hostile-input static and guest Tcl self-tests. |
| T-DL-02 | Elevation | `doas` and `300k-power` | high | mitigate | Locked live account, exact command+args rules, closed helper case statement, no shell delegation. |
| T-DL-03 | Tampering / Information Disclosure | Build inputs and signing key | high | mitigate | Retain clean commit, deterministic source archive, external private key, closed keyring, signed content-addressed repository, and no-network install. |
| T-DL-04 | Tampering / Repudiation | Candidate promotion | high | mitigate | Quarantine candidate, independent hash/byte checks, closed smoke schema, exact evidence hashes, same-volume atomic rename, atomic pointer replacement, immutable old LKG. |
| T-DL-05 | Spoofing | Serial readiness | medium | mitigate | Exact unique ordered stage grammar; UI marker only after Tk mapping; terminal marker only after real xterm PTY assertions. |
| T-DL-06 | Denial of Service | X/session/QEMU lifecycle | medium | mitigate | Two X attempts, rescue tty/shell, finite 15-minute smoke, owned process lease and `finally` cleanup. |
| T-DL-07 | Spoofing / Tampering | Loopback QMP | low | mitigate | Dynamically allocated loopback-only endpoint, run-local ownership, QMP capability handshake, no external network device. |
| T-DL-08 | Repudiation | Deferred compatibility claims | medium | mitigate | Evidence and docs explicitly separate executed BIOS optical facts from unexecuted UEFI/USB/Docker/reproducibility/size/release claims. |
</threat_model>

<source_coverage>
| Source | Item | Task | Status |
| --- | --- | --- | --- |
| GOAL | One funny bootable x86_64 Alpine ISO with real local terminal before deadline | 1-3 | COVERED |
| CONTEXT | Preserve current commits and immutable old LKG until validation | 2-3 | COVERED |
| CONTEXT | Unprivileged user, guarded X/Openbox, full-screen original Tcl/Tk UI, persistent disclaimer | 1 | COVERED |
| CONTEXT | Composer never executes shell; at least 12 safe local jokes/intents and fixed utility controls | 1 | COVERED |
| CONTEXT | UI button and Ctrl+Alt+T open xterm into BusyBox ash | 1 | COVERED |
| CONTEXT | ROOTFS/X/UI/TERM serial evidence with credible PTY seam | 1-3 | COVERED |
| CONTEXT | Fast deadline iteration without the multi-hour recursive audit | 2 | COVERED |
| CONTEXT | Exactly one final direct BIOS `-nic none` smoke, screenshot, terminal facts, SHA/bytes | 2-3 | COVERED |
| CONTEXT | All named deferred lanes/ceremony remain unclaimed | 2-3 | COVERED AS DEFERRED |
| RESEARCH | Alpine profile_virt + apkovl + tty1/startx/Openbox/Tk/xterm architecture | 1-2 | COVERED |
| REQ | Roadmap phase-completion requirements span later phases and are not claimed by this quick deadline contract | none | EXCLUDED: scoped to planned phases |
</source_coverage>

<verification>
1. Fast pre-build: `tests/deadline/run.ps1 -Scope AllStatic` and `tests/phase1/run.ps1 -Scope Unit` pass from a clean committed tree.
2. Build: the single DeadlineMvp invocation returns a quarantined candidate with exact hash/bytes and passing fast structural inspection while old LATEST is byte-identical.
3. Direct runtime: the single BIOS optical QEMU process uses `-nic none`, yields ordered semantic stages and an actual non-root xterm PTY proof, and produces a nonblank screenshot.
4. Publication: only passing evidence promotes; pointer/candidate/evidence hashes agree; old ISO remains unchanged.
5. Truthfulness: summary and `docs/DEADLINE-MVP.md` distinguish executed facts from every deferred claim and report actual size.
</verification>

<success_criteria>
- Runtime/UI and builder/smoke code are committed before the integration build.
- Only one deadline integration build and one direct BIOS ISO smoke are invoked by this plan.
- The promoted ISO, if any, has exact retained SHA-256 and byte size evidence.
- BIOS boot reaches the mapped UI and actual xterm PTY proof offline as `chatgpt`, not root.
- The screenshot is structurally valid and visibly nonblank by pixel-variance measurement.
- The old `2c968e...` ISO never changes; LATEST changes only after every smoke contract passes.
- UEFI runtime, raw USB, Docker parity, second-build reproducibility, size optimization, exhaustive security, and general release certification remain explicitly deferred.
</success_criteria>

<output>
Create `.planning/quick/260827-se6-deadline-mvp-vertical-slice-preserve-cur/260827-se6-SUMMARY.md` when done.
</output>
